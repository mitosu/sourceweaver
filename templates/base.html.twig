<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}IronWhisper{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { padding-top: 56px; }
        .sidebar { height: 100vh; position: fixed; top: 56px; left: 0; width: 220px; background-color: #f8f9fa; padding-top: 1rem; }
        .content { margin-left: 240px; padding: 2rem; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">IronWhisper</a>
        <div class="d-flex">
            <span class="navbar-text text-white me-3">{{ app.user.email }}</span>
            <a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">Salir</a>
        </div>
    </div>
</nav>

<div class="sidebar">
    <ul class="nav flex-column">
        <li class="nav-item"><a class="nav-link" href="{{ path('dashboard') }}">Dashboard</a></li>
        <li class="nav-item"><a class="nav-link" href="/upload">Subir Documento</a></li>
        {% if 'ROLE_ADMIN' in app.user.roles %}
        <li class="nav-item"><a class="nav-link" href="/admin/users">Gestión de Usuarios</a></li>
        {% endif %}
    </ul>
</div>

<div class="content">
    {% block body %}{% endblock %}
</div>
</body>
</html>
