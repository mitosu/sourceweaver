<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}IRONWHISPER{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Sistema de gestión de documentos y soporte técnico">
    <meta name="author" content="IRONWHISPER">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f8f9fa;
        }

        /* Sidebar fijo */
        .sidebar {
            width: 240px;
            background-color: #343a40;
            transition: width 0.3s ease;
            overflow-x: hidden;
            z-index: 1040;
        }

        /* Sidebar colapsado */
        .sidebar-collapsed .sidebar {
            width: 80px;
        }

        .sidebar a {
            color: #dee2e6;
            text-decoration: none;
        }

        .sidebar a:hover {
            color: #212529;
            background-color: #e9ecef;
        }

        .sidebar .nav-link.active {
            color: #212529;
            background-color: #0d6efd;
            border-radius: 0.375rem;
        }

        .sidebar-collapsed .sidebar-label,
        .sidebar-collapsed .sidebar-title {
            display: none;
        }

        .sidebar-collapsed .toggle-icon {
            display: none !important;
        }

        /* Contenedor principal alineado con sidebar */
        #main-content {
            margin-left: 240px;
            transition: margin-left 0.3s ease;
        }

        .sidebar-collapsed #main-content {
            margin-left: 80px;
        }

        /* Topbar */
        header.navbar {
            position: sticky;
            top: 0;
            height: 60px;
            z-index: 1020;
            background-color: #fff;
            border-bottom: 1px solid #dee2e6;
            transition: all 0.3s ease;
        }

        /* Bordes rectos */
        .card, .btn, .form-control, .navbar, .modal-content, .alert {
            border-radius: 0 !important;
        }

        .nav-treeview {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
            padding-left: 1rem;
        }

        .nav-treeview .nav-link {
            font-size: 0.8rem;
            padding-right: 1rem;
        }

        .nav-item.has-treeview > .nav-link {
            cursor: pointer;
        }

        .nav-link .toggle-icon {
            transition: transform 0.3s ease;
        }

        .toggle-icon {
            transition: all 0.3s ease;
        }

        .nav-item.open > .nav-treeview {
            max-height: 1000px;
        }

        .account-user-avatar {
            width: 32px;
            height: 32px;
            overflow: hidden;
            border-radius: 50%;
        }

        .sidebar-title {
            font-size: 1.25rem;
            font-weight: bold;
        }

        .sidebar-label {
            font-size: 0.875rem;
        }

        .toggle-icon {
            font-size: 0.875rem;
            transition: transform 0.3s ease;
        }
        .sidebar .nav-item {
            margin-bottom: 0.5rem;
        }

        .sidebar-collapsed .nav-treeview {
            display: none !important;
            max-height: 0 !important;
            overflow: hidden;
            padding: 0 !important;
        }

        .sidebar .nav-link.active {
            background-color:rgb(69, 253, 13) !important;  /* Azul Bootstrap o cambia al que quieras */
            color: #0e0b0bff !important;
            font-weight: 500;
        }

        /* Asegura que los íconos también cambien de color */
        .sidebar .nav-link.active i {
            color: #110f0fff !important;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>

{% block page_content %}
    {% include 'partials/_sidebar.html.twig' %}

    <div id="main-content">
        {% include 'partials/_topbar.html.twig' %}

        <main class="px-4 py-4">
            {% include 'partials/_breadcrumbs.html.twig' %}
            {{ block('main_content') }}
        </main>
    </div>
{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}
<script>
    document.addEventListener('DOMContentLoaded', function () {
        const toggleBtn = document.getElementById('sidebarToggle');
        const body = document.body;

        toggleBtn?.addEventListener('click', function () {
            const collapsed = body.classList.toggle('sidebar-collapsed');

            // Al colapsar: cerrar todos los submenús abiertos y restaurar iconos
            if (collapsed) {
                document.querySelectorAll('.nav-item.has-treeview.open').forEach(item => {
                    item.classList.remove('open');

                    const icon = item.querySelector('.toggle-icon');
                    if (icon) {
                        icon.classList.remove('bi-chevron-down');
                        icon.classList.add('bi-chevron-left');
                    }
                });
            }
        });

        document.querySelectorAll('.nav-item.has-treeview > .nav-link').forEach(link => {
            link.addEventListener('click', function (e) {
                e.preventDefault();

                if (document.body.classList.contains('sidebar-collapsed')) return;

                const parent = this.closest('.nav-item.has-treeview');
                parent.classList.toggle('open');

                const icon = this.querySelector('.toggle-icon');
                if (icon) {
                    const isOpen = parent.classList.contains('open');
                    icon.classList.toggle('bi-chevron-left', !isOpen);
                    icon.classList.toggle('bi-chevron-down', isOpen);
                }
            });
        });
    });
</script>
{% endblock %}
</body>
</html>